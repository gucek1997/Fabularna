using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Stamina : MonoBehaviour
{
   /*
    private CharacterController playerControler;

 
    public Image PasekStaminy;
    public float poziomStaminy = 5;
    private float czasBiegania;

    void Start()
    {
        playerControler = GetComponent<CharacterController>();
        czasBiegania = poziomStaminy;
    }

    void Update()
    {
        aktualizacjaPaskaStaminy();
    }

 
 
    private void aktualizacjaPaskaStaminy()
    {
        if (playerControler.bieganie() && playerControler.poruszanie() && czasBiegania > -0.5f)
        {
            czasBiegania -= Time.deltaTime;
        }
        else
        {
            if (czasBiegania < poziomStaminy)
            {
                czasBiegania += Time.deltaTime;
            }
        }
        if (PasekStaminy != null)
        {
            PasekStaminy.fillAmount = czasBiegania / poziomStaminy;
        }

    }


    public bool brakStaminy()
    {
        return czasBiegania <= 0;
    }
  */


}
